import  { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    logo: {
        height: 35,
        resizeMode: "contain",
        alignSelf: 'center',
        marginTop: 40,
        marginBottom: 15
    }
})

export default styles;